#pragma once

#define PROP_NECDRIVE		TEXT("_NecDrive_Object_")
#define PROP_DEVICE			TEXT("_Device_Name_")
#define PROP_FORCE          TEXT("_Force_")

LRESULT CALLBACK RPCDialog(HWND, UINT, WPARAM, LPARAM);
